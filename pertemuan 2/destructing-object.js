const user = {
    name: "Budi Santoso",
    age:20,
    major:"informatika",
};

// destructing object berdasarkan key
// const {name, age, major } = user;

const name = user.name;
const umur = user.age;

console,log(name);
